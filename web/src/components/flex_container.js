import React from 'react'
import '../styles/flex_container.css';

function FlexContainer({children}) {
    return (
        <div id="flex-container">{children}</div>
    )
}

export default FlexContainer
